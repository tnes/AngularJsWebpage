'use strict';

describe('myApp.contactPage module', function() {

    beforeEach(module('myApp.contactPage'));

    describe('contactPage controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var contactPageCtrl = $controller('contactPageCtrl');
            expect(contactPageCtrl).toBeDefined();
        }));

    });
});